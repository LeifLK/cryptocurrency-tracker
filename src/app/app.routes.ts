import { Routes } from '@angular/router';
import { CoinListComponent } from './CoinList/coin-list.component';

export const routes: Routes = [
    {
        path: '',
        component: CoinListComponent
    }
];
